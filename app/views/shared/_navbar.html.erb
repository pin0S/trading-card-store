<nav class="navbar navbar-expand-lg navbar-light nav">
      <%= link_to image_tag('logo.svg', :class => 'navbar-brand logo'), cards_path %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-stretch" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <%= link_to 'Cards', cards_path, class: "nav-link" %>
          
            <%= link_to 'Sell', new_card_path, class: "nav-link" %> 
          
          <% if user_signed_in? %>
            <%= link_to 'Account', account_path, class: "nav-link" %>
            <%= link_to 'Logout', destroy_user_session_path, 
          method: :delete, class: "nav-link" %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: "nav-link" %> 
            <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %>
          <%end%>
          </div>
          <div class="navbar-nav ml-auto">
            <% if user_signed_in? %>
                <% if get_unread_messages(current_user) != 0 %>
                  <%= link_to 'Inbox', conversations_path, class: "nav-link float-right justify-content-end btn-warning" %>
                <% else %>
                  <%= link_to 'Inbox', conversations_path, class: "nav-link float-right justify-content-end" %>  
                 <%end%>
            <%end%>
          </div>
        </div>
      </div>
 </nav>